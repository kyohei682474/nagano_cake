顧客の編集画面

<h1>編集画面</h1>

<%= form_with model: @customer, url:customer_registration_path(@customer.id), method: :patch do |f| %>


  <h4>本文</h4>
  <%= f.text_area :address %>

  <%= f.submit '保存' %>

<% end %>