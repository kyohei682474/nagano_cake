管理者商品編集画面

<h1>編集画面</h1>

<%= form_with model: @item,url:admin_item_path ,method: :patch do |f| %>

  <label for="inputImage">商品画面</label>
  <%= f.file_field :image, placeholder: "プロフィール画像", accept: "image/*" %>

  <h4>タイトル</h4>
  <%= f.text_field :name %>

  <h4>本文</h4>
  <%= f.text_area :introduction %>

  <h4>ジャンル</h4>
  <%= f.select :genre_id, [['--選択してください--',0],['和菓子',1],['焼き菓子',2],['洋菓子',3]], { include_blank: true, selected: 0 } %>

  <h4>税抜き価格</h4>
  <p><%= f.text_field :price %>円</p>

  <h4><%= f.label "販売ステータス" %></h4>
    <%= f.check_box :is_active,{},true%><%= f.label :is_active, '販売中' %>
    <%= f.check_box :is_active,{},false %><%= f.label :is_active, "販売停止" %>

  <%= f.submit '保存' %>

<% end %>